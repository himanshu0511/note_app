<script type="text/javascript">
  var page = {
      constants:{
          urls:{
              fetch_list_of_notes_url: "<%= url_for(:controller => 'notes', :action => 'user_note_list') %>",
              validate_sharing_email_list_url: "<%= url_for(:controller => 'notes', :action => 'validate_sharing_list') %>"
          }
      }
  };
  //execute NoteModule js on page load
  new NoteModule().init(page);

</script>