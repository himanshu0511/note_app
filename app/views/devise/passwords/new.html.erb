<div class="main_sub_content">
  <h2 class="user_form_heading">Forgot your password?</h2>

  <div class="user_form_container">

    <%= form_for(resource, as: resource_name, url: user_password_path) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true %>
        </div>

        <div class="actions">
          <%= f.submit "Send me reset password instructions", :class => "custom_button" %>
        </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</div>