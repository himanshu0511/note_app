<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "NoteApp" %></title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <div class="row">
    <%= render :partial => 'commons/top_navigation_and_search_bar' %>
  </div>
  <div class="row">
    <%= yield %>
  </div>
</div>
</body>
</html>
